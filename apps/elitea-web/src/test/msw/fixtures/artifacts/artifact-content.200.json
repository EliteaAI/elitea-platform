{
  "recordedAt": "2026-08-08",
  "synthetic": true,
  "note": "Synthetic: GET /api/v2/artifacts/objects/{projectID}/{bucket}/{key...} response body (router.go:288 -> Handler.DownloadObject). The body is the object's own bytes, so any content is representative.",
  "body": "hello artifact content"
}
