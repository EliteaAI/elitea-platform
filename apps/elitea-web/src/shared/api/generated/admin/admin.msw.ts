/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Elitea Platform API
 * External REST API for the Elitea Platform (elitea-main Go service).
 * Wire-compatible drop-in replacement for pylon_main (Python/Flask).
 *
 * ## Response shape conventions
 * - **Single resource**: returned directly at top level — `{"id": 42, "name": "..."}`
 * - **General errors (4xx/5xx)**: `{"error": "message"}` (pkg/apierr/apierr.go:47-59
 *   and the handlers' inline `map[string]any{"error": ...}` writes). One
 *   documented shape deviates: the five social/handler.go 500 sites (Like
 *   :268, Unlike :301, Pin :321, Unpin :341, CreateFeedback :419) emit the
 *   pylon-era `{"ok": false, "error": ...}` envelope instead, modeled as
 *   the named `SocialActionErrorResponse` schema and referenced only on
 *   those specific operations. Five more UNDOCUMENTED sites of the same
 *   envelope remain on toolkits/handler.go (:286, 312, 324, 350, 362) —
 *   scope that shape there when those domains are spec'd.
 * - **Rate limit (429)**: intentionally NOT documented. The Go RateLimit
 *   middleware is a pass-through stub (internal/api/middleware/ratelimit.go:9-14)
 *   and no elitea-main code emits a 429 body — documenting one would be contract
 *   fiction. Re-add per-operation 429 responses (pylon shape:
 *   `{"error": {"message", "type": "rate_limit_error", "code":
 *   "rate_limit_exceeded"}}`) when rate limiting becomes real.
 * - Field names are **snake_case** unless the Go handler observably marshals
 *   otherwise (e.g. `lastModified` in the S3 listing, `DisplayName`/`ID` in the
 *   S3 owner block).
 *
 * ## NOTE(W2) — field-level enrichment conventions
 * Every schema below is derived from the Go handler that actually marshals the
 * response (UI reimplementation spec §5.1 / §9.3 unit W2). Evidence is recorded
 * per schema in `NOTE(W2)` comments/descriptions with file:line references.
 * Where a handler genuinely passes through arbitrary JSON (DB `jsonb` columns,
 * echoed request bodies), the schema carries an
 * `x-elitea-passthrough: "NOTE(W2): ..."` marker justifying the permissive
 * `additionalProperties`. A permissive object WITHOUT that marker is a defect —
 * `apps/elitea-web/scripts/check-contract-coverage.mjs` fails it, wired as the
 * "Contract coverage (§5.1)" step of ci-web.yml's `gates` job.
 *
 * OpenAPI spec version: 2.0.0
 */
import { faker } from "@faker-js/faker";

import { HttpResponse, delay, http } from "msw";
import type { RequestHandlerOptions } from "msw";

import type {
  MessageResponse,
  ModerationStatusResponse,
  PlatformSettings,
  Role,
  SupportAssistantConfig,
  UserInviteResult,
  UserListResponse,
} from "../model";

export const getUserListResponseMock = (
  overrideResponse: Partial<Extract<UserListResponse, object>> = {},
): UserListResponse => ({
  rows: Array.from(
    { length: faker.number.int({ min: 1, max: 10 }) },
    (_, i) => i + 1,
  ).map(() => ({
    id: faker.string.alpha({ length: { min: 10, max: 20 } }),
    email: faker.string.alpha({ length: { min: 10, max: 20 } }),
    name: faker.string.alpha({ length: { min: 10, max: 20 } }),
    roles: Array.from(
      { length: faker.number.int({ min: 1, max: 10 }) },
      (_, i) => i + 1,
    ).map(() => faker.string.alpha({ length: { min: 10, max: 20 } })),
  })),
  total: faker.number.int(),
  ...overrideResponse,
});

export const getUserCreateResponseMock = (): UserInviteResult[] =>
  Array.from(
    { length: faker.number.int({ min: 1, max: 10 }) },
    (_, i) => i + 1,
  ).map(() => ({
    msg: faker.string.alpha({ length: { min: 10, max: 20 } }),
    status: faker.helpers.arrayElement(["ok", "error"] as const),
    email: faker.string.alpha({ length: { min: 10, max: 20 } }),
    id: faker.helpers.arrayElement([
      faker.string.alpha({ length: { min: 10, max: 20 } }),
      undefined,
    ]),
  }));

export const getUserUpdateResponseMock = (
  overrideResponse: Partial<Extract<MessageResponse, object>> = {},
): MessageResponse => ({
  msg: faker.string.alpha({ length: { min: 10, max: 20 } }),
  ...overrideResponse,
});

export const getUserDeleteResponseMock = (
  overrideResponse: Partial<Extract<MessageResponse, object>> = {},
): MessageResponse => ({
  msg: faker.string.alpha({ length: { min: 10, max: 20 } }),
  ...overrideResponse,
});

export const getRoleListResponseMock = (): Role[] =>
  Array.from(
    { length: faker.number.int({ min: 1, max: 10 }) },
    (_, i) => i + 1,
  ).map(() => ({
    id: faker.string.alpha({ length: { min: 10, max: 20 } }),
    name: faker.string.alpha({ length: { min: 10, max: 20 } }),
  }));

export const getModerationStatusResponseMock = (
  overrideResponse: Partial<Extract<ModerationStatusResponse, object>> = {},
): ModerationStatusResponse => ({
  status: faker.string.alpha({ length: { min: 10, max: 20 } }),
  ...overrideResponse,
});

export const getCreateModerationRequestResponseMock = (
  overrideResponse: Partial<Extract<ModerationStatusResponse, object>> = {},
): ModerationStatusResponse => ({
  status: faker.string.alpha({ length: { min: 10, max: 20 } }),
  ...overrideResponse,
});

export const getGetPlatformSettingsResponseMock = (
  overrideResponse: Partial<Extract<PlatformSettings, object>> = {},
): PlatformSettings => ({
  chat_enabled: faker.datatype.boolean(),
  applications_enabled: faker.datatype.boolean(),
  skills_enabled: faker.datatype.boolean(),
  toolkits_enabled: faker.datatype.boolean(),
  datasources_enabled: faker.datatype.boolean(),
  pipelines_enabled: faker.datatype.boolean(),
  publishing_enabled: faker.datatype.boolean(),
  moderation_enabled: faker.datatype.boolean(),
  mcp_enabled: faker.datatype.boolean(),
  support_chat_enabled: faker.datatype.boolean(),
  mcp_in_menu_enabled: faker.helpers.arrayElement([
    faker.datatype.boolean(),
    undefined,
  ]),
  voice_features_enabled: faker.helpers.arrayElement([
    faker.datatype.boolean(),
    undefined,
  ]),
  voice_features_temporarily_disabled: faker.helpers.arrayElement([
    faker.datatype.boolean(),
    undefined,
  ]),
  ...overrideResponse,
});

export const getGetSupportAssistantConfigResponseMock = (
  overrideResponse: Partial<Extract<SupportAssistantConfig, object>> = {},
): SupportAssistantConfig => ({
  enabled: faker.datatype.boolean(),
  ...overrideResponse,
});

export const getUserListMockHandler = (
  overrideResponse?:
    | UserListResponse
    | ((
        info: Parameters<Parameters<typeof http.get>[1]>[0],
      ) => Promise<UserListResponse> | UserListResponse),
  options?: RequestHandlerOptions,
) => {
  return http.get(
    "*/admin/users/default/:projectId",
    async (info: Parameters<Parameters<typeof http.get>[1]>[0]) => {
      await delay(0);

      return HttpResponse.json(
        overrideResponse !== undefined
          ? typeof overrideResponse === "function"
            ? await overrideResponse(info)
            : overrideResponse
          : getUserListResponseMock(),
        { status: 200 },
      );
    },
    options,
  );
};

export const getUserCreateMockHandler = (
  overrideResponse?:
    | UserInviteResult[]
    | ((
        info: Parameters<Parameters<typeof http.post>[1]>[0],
      ) => Promise<UserInviteResult[]> | UserInviteResult[]),
  options?: RequestHandlerOptions,
) => {
  return http.post(
    "*/admin/users/default/:projectId",
    async (info: Parameters<Parameters<typeof http.post>[1]>[0]) => {
      await delay(0);

      return HttpResponse.json(
        overrideResponse !== undefined
          ? typeof overrideResponse === "function"
            ? await overrideResponse(info)
            : overrideResponse
          : getUserCreateResponseMock(),
        { status: 200 },
      );
    },
    options,
  );
};

export const getUserUpdateMockHandler = (
  overrideResponse?:
    | MessageResponse
    | ((
        info: Parameters<Parameters<typeof http.put>[1]>[0],
      ) => Promise<MessageResponse> | MessageResponse),
  options?: RequestHandlerOptions,
) => {
  return http.put(
    "*/admin/users/default/:projectId",
    async (info: Parameters<Parameters<typeof http.put>[1]>[0]) => {
      await delay(0);

      return HttpResponse.json(
        overrideResponse !== undefined
          ? typeof overrideResponse === "function"
            ? await overrideResponse(info)
            : overrideResponse
          : getUserUpdateResponseMock(),
        { status: 200 },
      );
    },
    options,
  );
};

export const getUserDeleteMockHandler = (
  overrideResponse?:
    | MessageResponse
    | ((
        info: Parameters<Parameters<typeof http.delete>[1]>[0],
      ) => Promise<MessageResponse> | MessageResponse),
  options?: RequestHandlerOptions,
) => {
  return http.delete(
    "*/admin/users/default/:projectId",
    async (info: Parameters<Parameters<typeof http.delete>[1]>[0]) => {
      await delay(0);

      return HttpResponse.json(
        overrideResponse !== undefined
          ? typeof overrideResponse === "function"
            ? await overrideResponse(info)
            : overrideResponse
          : getUserDeleteResponseMock(),
        { status: 200 },
      );
    },
    options,
  );
};

export const getRoleListMockHandler = (
  overrideResponse?:
    | Role[]
    | ((
        info: Parameters<Parameters<typeof http.get>[1]>[0],
      ) => Promise<Role[]> | Role[]),
  options?: RequestHandlerOptions,
) => {
  return http.get(
    "*/admin/roles/default/:projectId",
    async (info: Parameters<Parameters<typeof http.get>[1]>[0]) => {
      await delay(0);

      return HttpResponse.json(
        overrideResponse !== undefined
          ? typeof overrideResponse === "function"
            ? await overrideResponse(info)
            : overrideResponse
          : getRoleListResponseMock(),
        { status: 200 },
      );
    },
    options,
  );
};

export const getModerationStatusMockHandler = (
  overrideResponse?:
    | ModerationStatusResponse
    | ((
        info: Parameters<Parameters<typeof http.get>[1]>[0],
      ) => Promise<ModerationStatusResponse> | ModerationStatusResponse),
  options?: RequestHandlerOptions,
) => {
  return http.get(
    "*/admin/moderation_status/default/:projectId/:entityId",
    async (info: Parameters<Parameters<typeof http.get>[1]>[0]) => {
      await delay(0);

      return HttpResponse.json(
        overrideResponse !== undefined
          ? typeof overrideResponse === "function"
            ? await overrideResponse(info)
            : overrideResponse
          : getModerationStatusResponseMock(),
        { status: 200 },
      );
    },
    options,
  );
};

export const getCreateModerationRequestMockHandler = (
  overrideResponse?:
    | ModerationStatusResponse
    | ((
        info: Parameters<Parameters<typeof http.post>[1]>[0],
      ) => Promise<ModerationStatusResponse> | ModerationStatusResponse),
  options?: RequestHandlerOptions,
) => {
  return http.post(
    "*/admin/moderation_status/default/:projectId/:entityId",
    async (info: Parameters<Parameters<typeof http.post>[1]>[0]) => {
      await delay(0);

      return HttpResponse.json(
        overrideResponse !== undefined
          ? typeof overrideResponse === "function"
            ? await overrideResponse(info)
            : overrideResponse
          : getCreateModerationRequestResponseMock(),
        { status: 200 },
      );
    },
    options,
  );
};

export const getGetPlatformSettingsMockHandler = (
  overrideResponse?:
    | PlatformSettings
    | ((
        info: Parameters<Parameters<typeof http.get>[1]>[0],
      ) => Promise<PlatformSettings> | PlatformSettings),
  options?: RequestHandlerOptions,
) => {
  return http.get(
    "*/elitea_core/platform_settings/prompt_lib",
    async (info: Parameters<Parameters<typeof http.get>[1]>[0]) => {
      await delay(0);

      return HttpResponse.json(
        overrideResponse !== undefined
          ? typeof overrideResponse === "function"
            ? await overrideResponse(info)
            : overrideResponse
          : getGetPlatformSettingsResponseMock(),
        { status: 200 },
      );
    },
    options,
  );
};

export const getGetSupportAssistantConfigMockHandler = (
  overrideResponse?:
    | SupportAssistantConfig
    | ((
        info: Parameters<Parameters<typeof http.get>[1]>[0],
      ) => Promise<SupportAssistantConfig> | SupportAssistantConfig),
  options?: RequestHandlerOptions,
) => {
  return http.get(
    "*/support_assistant/config",
    async (info: Parameters<Parameters<typeof http.get>[1]>[0]) => {
      await delay(0);

      return HttpResponse.json(
        overrideResponse !== undefined
          ? typeof overrideResponse === "function"
            ? await overrideResponse(info)
            : overrideResponse
          : getGetSupportAssistantConfigResponseMock(),
        { status: 200 },
      );
    },
    options,
  );
};
export const getAdminMock = () => [
  getUserListMockHandler(),
  getUserCreateMockHandler(),
  getUserUpdateMockHandler(),
  getUserDeleteMockHandler(),
  getRoleListMockHandler(),
  getModerationStatusMockHandler(),
  getCreateModerationRequestMockHandler(),
  getGetPlatformSettingsMockHandler(),
  getGetSupportAssistantConfigMockHandler(),
];
