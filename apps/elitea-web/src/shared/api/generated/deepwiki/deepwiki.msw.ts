/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Elitea Platform API
 * External REST API for the Elitea Platform (elitea-main Go service).
 * Wire-compatible drop-in replacement for pylon_main (Python/Flask).
 *
 * ## Response shape conventions
 * - **Single resource**: returned directly at top level — `{"id": 42, "name": "..."}`
 * - **General errors (4xx/5xx)**: `{"error": "message"}` (pkg/apierr/apierr.go:47-59
 *   and the handlers' inline `map[string]any{"error": ...}` writes). One
 *   documented shape deviates: the five social/handler.go 500 sites (Like
 *   :268, Unlike :301, Pin :321, Unpin :341, CreateFeedback :419) emit the
 *   pylon-era `{"ok": false, "error": ...}` envelope instead, modeled as
 *   the named `SocialActionErrorResponse` schema and referenced only on
 *   those specific operations. Five more UNDOCUMENTED sites of the same
 *   envelope remain on toolkits/handler.go (:286, 312, 324, 350, 362) —
 *   scope that shape there when those domains are spec'd.
 * - **Rate limit (429)**: intentionally NOT documented. The Go RateLimit
 *   middleware is a pass-through stub (internal/api/middleware/ratelimit.go:9-14)
 *   and no elitea-main code emits a 429 body — documenting one would be contract
 *   fiction. Re-add per-operation 429 responses (pylon shape:
 *   `{"error": {"message", "type": "rate_limit_error", "code":
 *   "rate_limit_exceeded"}}`) when rate limiting becomes real.
 * - Field names are **snake_case** unless the Go handler observably marshals
 *   otherwise (e.g. `lastModified` in the S3 listing, `DisplayName`/`ID` in the
 *   S3 owner block).
 *
 * ## NOTE(W2) — field-level enrichment conventions
 * Every schema below is derived from the Go handler that actually marshals the
 * response (UI reimplementation spec §5.1 / §9.3 unit W2). Evidence is recorded
 * per schema in `NOTE(W2)` comments/descriptions with file:line references.
 * Where a handler genuinely passes through arbitrary JSON (DB `jsonb` columns,
 * echoed request bodies), the schema carries an
 * `x-elitea-passthrough: "NOTE(W2): ..."` marker justifying the permissive
 * `additionalProperties`. A permissive object WITHOUT that marker is a defect —
 * `apps/elitea-web/scripts/check-contract-coverage.mjs` fails it, wired as the
 * "Contract coverage (§5.1)" step of ci-web.yml's `gates` job.
 *
 * OpenAPI spec version: 2.0.0
 */
import { faker } from "@faker-js/faker";

import { HttpResponse, delay, http } from "msw";
import type { RequestHandlerOptions } from "msw";

import type {
  DeepWikiInvocationAccepted,
  DeepWikiInvocationStatus,
  DeepWikiSlots,
} from "../model";

export const getGetDeepWikiSlotsResponseMock = (
  overrideResponse: Partial<Extract<DeepWikiSlots, object>> = {},
): DeepWikiSlots => ({
  available: faker.number.int(),
  total: faker.number.int(),
  active: faker.number.int(),
  can_start: faker.datatype.boolean(),
  canStart: faker.helpers.arrayElement([faker.datatype.boolean(), undefined]),
  mode: faker.string.alpha({ length: { min: 10, max: 20 } }),
  note: faker.helpers.arrayElement([
    faker.string.alpha({ length: { min: 10, max: 20 } }),
    undefined,
  ]),
  namespace: faker.helpers.arrayElement([
    faker.string.alpha({ length: { min: 10, max: 20 } }),
    undefined,
  ]),
  error: faker.helpers.arrayElement([
    faker.string.alpha({ length: { min: 10, max: 20 } }),
    undefined,
  ]),
  ...overrideResponse,
});

export const getInvokeDeepWikiToolResponseMock = (
  overrideResponse: Partial<Extract<DeepWikiInvocationAccepted, object>> = {},
): DeepWikiInvocationAccepted => ({
  invocation_id: faker.string.alpha({ length: { min: 10, max: 20 } }),
  status: faker.helpers.arrayElement(["Started"] as const),
  ...overrideResponse,
});

export const getGetDeepWikiInvocationResponseMock = (
  overrideResponse: Partial<Extract<DeepWikiInvocationStatus, object>> = {},
): DeepWikiInvocationStatus => ({
  invocation_id: faker.helpers.arrayElement([
    faker.string.alpha({ length: { min: 10, max: 20 } }),
    undefined,
  ]),
  status: faker.helpers.arrayElement([
    "Started",
    "InProgress",
    "Completed",
    "Error",
  ] as const),
  result: faker.helpers.arrayElement([
    faker.string.alpha({ length: { min: 10, max: 20 } }),
    undefined,
  ]),
  result_type: faker.helpers.arrayElement([
    faker.helpers.arrayElement(["String"] as const),
    undefined,
  ]),
  error_category: faker.helpers.arrayElement([
    faker.string.alpha({ length: { min: 10, max: 20 } }),
    undefined,
  ]),
  error_type: faker.helpers.arrayElement([
    faker.string.alpha({ length: { min: 10, max: 20 } }),
    undefined,
  ]),
  custom_events: faker.helpers.arrayElement([
    Array.from(
      { length: faker.number.int({ min: 1, max: 10 }) },
      (_, i) => i + 1,
    ).map(() => ({
      data: faker.helpers.arrayElement([
        {
          message: faker.helpers.arrayElement([
            faker.string.alpha({ length: { min: 10, max: 20 } }),
            undefined,
          ]),
        },
        undefined,
      ]),
    })),
    undefined,
  ]),
  ...overrideResponse,
});

export const getGetDeepWikiSlotsMockHandler = (
  overrideResponse?:
    | DeepWikiSlots
    | ((
        info: Parameters<Parameters<typeof http.get>[1]>[0],
      ) => Promise<DeepWikiSlots> | DeepWikiSlots),
  options?: RequestHandlerOptions,
) => {
  return http.get(
    "*/deepwiki/slots/:projectId",
    async (info: Parameters<Parameters<typeof http.get>[1]>[0]) => {
      await delay(0);

      return HttpResponse.json(
        overrideResponse !== undefined
          ? typeof overrideResponse === "function"
            ? await overrideResponse(info)
            : overrideResponse
          : getGetDeepWikiSlotsResponseMock(),
        { status: 200 },
      );
    },
    options,
  );
};

export const getInvokeDeepWikiToolMockHandler = (
  overrideResponse?:
    | DeepWikiInvocationAccepted
    | ((
        info: Parameters<Parameters<typeof http.post>[1]>[0],
      ) => Promise<DeepWikiInvocationAccepted> | DeepWikiInvocationAccepted),
  options?: RequestHandlerOptions,
) => {
  return http.post(
    "*/deepwiki/tools/:projectId/:toolkitName/:toolName/invoke",
    async (info: Parameters<Parameters<typeof http.post>[1]>[0]) => {
      await delay(0);

      return HttpResponse.json(
        overrideResponse !== undefined
          ? typeof overrideResponse === "function"
            ? await overrideResponse(info)
            : overrideResponse
          : getInvokeDeepWikiToolResponseMock(),
        { status: 200 },
      );
    },
    options,
  );
};

export const getGetDeepWikiInvocationMockHandler = (
  overrideResponse?:
    | DeepWikiInvocationStatus
    | ((
        info: Parameters<Parameters<typeof http.get>[1]>[0],
      ) => Promise<DeepWikiInvocationStatus> | DeepWikiInvocationStatus),
  options?: RequestHandlerOptions,
) => {
  return http.get(
    "*/deepwiki/invocations/:projectId/:toolkitName/:toolName/:invocationId",
    async (info: Parameters<Parameters<typeof http.get>[1]>[0]) => {
      await delay(0);

      return HttpResponse.json(
        overrideResponse !== undefined
          ? typeof overrideResponse === "function"
            ? await overrideResponse(info)
            : overrideResponse
          : getGetDeepWikiInvocationResponseMock(),
        { status: 200 },
      );
    },
    options,
  );
};

export const getCancelDeepWikiInvocationMockHandler = (
  overrideResponse?:
    | void
    | ((
        info: Parameters<Parameters<typeof http.delete>[1]>[0],
      ) => Promise<void> | void),
  options?: RequestHandlerOptions,
) => {
  return http.delete(
    "*/deepwiki/invocations/:projectId/:toolkitName/:toolName/:invocationId",
    async (info: Parameters<Parameters<typeof http.delete>[1]>[0]) => {
      await delay(0);
      if (typeof overrideResponse === "function") {
        await overrideResponse(info);
      }

      return new HttpResponse(null, { status: 204 });
    },
    options,
  );
};
export const getDeepwikiMock = () => [
  getGetDeepWikiSlotsMockHandler(),
  getInvokeDeepWikiToolMockHandler(),
  getGetDeepWikiInvocationMockHandler(),
  getCancelDeepWikiInvocationMockHandler(),
];
