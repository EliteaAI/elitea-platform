/**
 * Shared constants — copied from the old-app's `[fsd]/shared/lib/constants/`.
 */

export const AccordionConstants = {
  AccordionShowMode: {
    RightMode: 'right',
    LeftMode: 'left',
  },
} as const;
