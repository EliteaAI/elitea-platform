/**
 * pages/help-center — Help Center page (Wave-2 unit A13, issue #26).
 *
 * Ported from `apps/elitea-ui/src/[fsd]/pages/resources/` (index.jsx +
 * ResourceCard.jsx + ResourceVersionInfo.jsx).
 *
 * Spec §9.2: the page renders a grid of resource cards driven by
 * RESOURCE_CARD_CONFIGS (hardcoded fallback defaults; see
 * `lib/ResourceCardConfig.ts` for the API gap).
 *
 * Routes: **NOT wired here** (out of scope per issue #26).
 */
export { default as HelpCenterPage } from './HelpCenterPage';
