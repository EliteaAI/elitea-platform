# The lockfile is COMMITTED (spec D4) — do not add package-lock.json here.
node_modules/
dist/
coverage/
coverage-shards/
test-results/

# per-fixture-case configs generated (and normally cleaned) by
# scripts/check-gates-selftest.mjs — never commit a crash leftover
.oxlintrc.gen.json
