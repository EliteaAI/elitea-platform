// Issue #189: the theme gate must not read an issue reference as a colour.
export const listNote = 'NOTE (issue #130): the write verbs have their own handlers.';
// The title form, taken from the second live instance of the same defect.
export const caseTitle = '#413 — a failed list read renders as an error';
