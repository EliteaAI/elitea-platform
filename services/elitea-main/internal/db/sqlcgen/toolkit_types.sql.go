// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: toolkit_types.sql

package sqlcgen

import (
	"context"
)

const listCurrentToolkitTypes = `-- name: ListCurrentToolkitTypes :many

SELECT DISTINCT type
FROM elitea_tools
WHERE (
        (
          $1::boolean
          AND (
            COALESCE((meta ->> 'mcp')::boolean, false)
            OR type = 'mcp'
          )
        )
        OR (
          NOT $1::boolean
          AND NOT COALESCE((meta ->> 'mcp')::boolean, false)
          AND type <> 'mcp'
        )
      )
  AND (
        (
          $2::boolean
          AND (
            COALESCE((meta ->> 'application')::boolean, false)
            OR type = 'application'
          )
        )
        OR (
          NOT $2::boolean
          AND NOT COALESCE((meta ->> 'application')::boolean, false)
          AND type <> 'application'
        )
      )
ORDER BY type ASC
`

type ListCurrentToolkitTypesParams struct {
	FilterMcp         bool `db:"filter_mcp" json:"filter_mcp"`
	FilterApplication bool `db:"filter_application" json:"filter_application"`
}

// The unqualified table name is intentional. This query runs only inside an
// authorized project transaction whose local search_path is p_<project_id>.
//
// The two predicates are independent to preserve the current endpoint:
// requesting MCP rows does not implicitly include application rows, and
// requesting application rows does not implicitly include MCP rows.
func (q *Queries) ListCurrentToolkitTypes(ctx context.Context, arg ListCurrentToolkitTypesParams) ([]string, error) {
	rows, err := q.db.Query(ctx, listCurrentToolkitTypes, arg.FilterMcp, arg.FilterApplication)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var type_ string
		if err := rows.Scan(&type_); err != nil {
			return nil, err
		}
		items = append(items, type_)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
