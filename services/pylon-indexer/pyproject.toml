[project]
name = "pylon-indexer"
version = "0.1.0"
description = "EliteA indexer service — agent runtime & SDK execution (pylon-based)"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
