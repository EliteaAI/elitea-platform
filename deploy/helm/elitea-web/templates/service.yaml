apiVersion: v1
kind: Service
metadata:
  name: {{ include "elitea-web.fullname" . }}
  labels:
    {{- include "elitea-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "elitea-web.selectorLabels" . | nindent 4 }}
