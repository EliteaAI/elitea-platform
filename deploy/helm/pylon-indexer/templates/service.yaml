apiVersion: v1
kind: Service
metadata:
  name: {{ include "pylon-indexer.fullname" . }}
  labels:
    {{- include "pylon-indexer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "pylon-indexer.selectorLabels" . | nindent 4 }}
