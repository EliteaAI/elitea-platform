# Nested under `main:` — this file configured the elitea-main chart when the
# platform shipped as eight charts; it configures the same component here.
main:
  replicaCount: 1

  image:
    tag: "shadow-v0.1"

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 500m
      memory: 256Mi

  env:
    DATABASE_URL: "postgres://elitea:elitea@postgres:5432/elitea?sslmode=disable"
    REDIS_URL: "redis:6379"
    SHADOW_ENABLED: "true"
    SHADOW_LEGACY_URL: "http://pylon-main:8000"
    SHADOW_WEIGHT: "0.1"
    LEGACY_URL: "http://pylon-main:8000"
    CANARY_WEIGHT: "0.0"

  autoscaling:
    enabled: false

  podDisruptionBudget:
    enabled: false
