{{- if .Values.llmGateway.enabled }}
{{- if .Values.llmGateway.ingress.enabled }}
{{- if not .Values.llmGateway.ingress.acknowledgeMTLSBypass }}
{{- fail "ingress.enabled=true routes external traffic straight to the mTLS-internal-only Service (templates/service.yaml — \"NEVER public\"). Set ingress.acknowledgeMTLSBypass=true only once your ingress/Gateway itself terminates and re-verifies mTLS equivalent to elitea-main." }}
{{- end }}
{{- if .Values.llmGateway.ingress.gatewayApi }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "elitea-llm-gateway.fullname" . }}
  labels:
    {{- include "elitea-llm-gateway.labels" . | nindent 4 }}
  {{- with .Values.llmGateway.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    - name: {{ .Values.llmGateway.ingress.gateway.name }}
      {{- if .Values.llmGateway.ingress.gateway.namespace }}
      namespace: {{ .Values.llmGateway.ingress.gateway.namespace }}
      {{- end }}
      {{- if .Values.llmGateway.ingress.gateway.sectionName }}
      sectionName: {{ .Values.llmGateway.ingress.gateway.sectionName }}
      {{- end }}
  {{- with .Values.llmGateway.ingress.hosts }}
  hostnames:
    {{- range . }}
    - {{ . | quote }}
    {{- end }}
  {{- end }}
  rules:
    - matches:
        - path:
            type: {{ .Values.llmGateway.ingress.pathType }}
            value: {{ .Values.llmGateway.ingress.path }}
      backendRefs:
        - name: {{ .Values.llmGateway.service.name }}
          port: {{ .Values.llmGateway.service.port }}
{{- else }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "elitea-llm-gateway.fullname" . }}
  labels:
    {{- include "elitea-llm-gateway.labels" . | nindent 4 }}
  {{- with .Values.llmGateway.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.llmGateway.ingress.className }}
  ingressClassName: {{ .Values.llmGateway.ingress.className }}
  {{- end }}
  {{- with .Values.llmGateway.ingress.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.llmGateway.ingress.hosts }}
    - host: {{ . | quote }}
      http:
        paths:
          - path: {{ $.Values.llmGateway.ingress.path }}
            pathType: {{ $.Values.llmGateway.ingress.pathType }}
            backend:
              service:
                name: {{ $.Values.llmGateway.service.name }}
                port:
                  number: {{ $.Values.llmGateway.service.port }}
    {{- end }}
{{- end }}
{{- end }}
{{- end }}
