# DEVELOPMENT TOPOLOGY ONLY. This listener is cleartext and has no request
# admission policy. Production requires TLS termination and network policy.
entryPoints:
  web:
    address: ":80"

providers:
  file:
    filename: /etc/traefik/dynamic.yml

log:
  level: INFO

accessLog: {}
