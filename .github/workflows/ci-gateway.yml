name: CI — Gateway

# elitea-llm-gateway is a STANDALONE Go module with a Go 1.26.4 FLOOR (required
# by bifrost/core), deliberately excluded from the root go.work (which stays on
# 1.25.8 for elitea-main/elitea-scheduler). It therefore needs its own CI job on
# a Go 1.26 runner, built with GOWORK=off so the workspace is never consulted.
#
# THE PIN BELOW IS THE SERIES `1.26`, NOT THE PATCH `1.26.4` (#506).
#
# services/elitea-llm-gateway/Containerfile builds from `golang:1.26-alpine`, so
# the released image takes each 1.26 patch. An exact patch pin here tested the
# gateway on a toolchain the image no longer uses: the scan in ci-image-scan.yml
# reported 9 HIGH findings against stdlib v1.26.4, all fixed in 1.26.6. A
# standard-library change that alters behaviour then reaches the release and
# never reaches the tests.
#
# `go 1.26.4` in go.mod states the MINIMUM the module needs. It is not a
# maximum. actions/setup-go reads "1.26" as the newest 1.26 patch, which is what
# the Containerfile does. scripts/ci/check-gateway-toolchain.sh holds the
# workflow, the Containerfile and go.mod together, and runs in the lint job.

on:
  pull_request:
    paths:
      - "services/elitea-llm-gateway/**"
      - ".github/workflows/ci-gateway.yml"
      # internal/sdkpin/pin_test.go compares sdk-pin.json against this lock
      # file, so a lock-file-only SDK bump is exactly the change that gate
      # exists to catch. Without this path the job never starts on it, and the
      # gate cannot run on its own subject (#518 again).
      - "services/elitea-worker-python/elitea-sdk.lock.json"
      # The toolchain gate below reads this workflow and the gateway
      # Containerfile. Both subjects are already named above; this line adds
      # the gate's OWN file, so an edit to the checker runs the checker (#518).
      - "scripts/ci/check-gateway-toolchain.sh"
  push:
    branches:
      - main
    paths:
      - "services/elitea-llm-gateway/**"
      # This workflow's own file. The pull_request arm above names it and this
      # arm did not, so a push that edited only the gate could not start the
      # gate it edited (#518).
      - ".github/workflows/ci-gateway.yml"
      - "services/elitea-worker-python/elitea-sdk.lock.json"
      - "scripts/ci/check-gateway-toolchain.sh"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  # Standalone module: never resolve the parent workspace.
  GOWORK: "off"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/elitea-llm-gateway
    steps:
      - uses: actions/checkout@v7

      # The gateway must be TESTED on the toolchain it SHIPS (#506). The pin
      # below and services/elitea-llm-gateway/Containerfile are two files that
      # nothing compared, so they drifted: this workflow asked for the exact
      # patch 1.26.4 while the image had already moved to golang:1.26-alpine.
      # This step is the comparison. It runs BEFORE setup-go, so a disagreement
      # is reported instead of installed, and it needs no toolchain of its own.
      - name: The workflow pin matches the shipped toolchain
        working-directory: .
        run: bash scripts/ci/check-gateway-toolchain.sh

      - uses: actions/setup-go@v7
        with:
          go-version: "1.26"
          cache-dependency-path: services/elitea-llm-gateway/go.sum
      - uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          working-directory: services/elitea-llm-gateway

  test:
    name: Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/elitea-llm-gateway
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: "1.26"
          cache-dependency-path: services/elitea-llm-gateway/go.sum

      - name: Go vet
        run: go vet ./...

      - name: Run tests
        run: go test -race -count=1 -coverprofile=coverage.out ./...

      - name: Coverage floor — enforcement packages
        # Fails if the budget-gate / governance / fail-mode / cost packages
        # regress below their current coverage. These are the money+correctness
        # core; a silent coverage drop there is how untested gaps slip in.
        run: ./scripts/coverage-floor.sh coverage.out

      - name: Env-drift gate — code env reads vs Helm chart
        # Fails if the code reads a required env var (os.Getenv, no default) that
        # the Helm chart cannot set. This class of bug (NATS_URL vs
        # GATEWAY_NATS_URL; missing GATEWAY_TLS_*) recurred twice in review and
        # no unit test catches it. yq is needed to read values.yaml.
        run: |
          command -v yq >/dev/null || { sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 && sudo chmod +x /usr/local/bin/yq; }
          ./scripts/env-drift-check.sh

      - name: Generate llmproxy cobertura coverage (BF0.6t gate)
        # Emits internal/llmproxy/coverage.xml consumed by the BF0.6t
        # `type: coverage` validator (>=85%). Uses a pinned go-run tool so the
        # gateway go.mod is not modified.
        run: ./scripts/coverage.sh

      - name: Upload coverage
        uses: actions/upload-artifact@v7
        with:
          name: gateway-coverage
          path: |
            services/elitea-llm-gateway/coverage.out
            services/elitea-llm-gateway/internal/llmproxy/coverage.xml

  build:
    name: Build
    # CI validation only — confirms the gateway binary compiles cleanly on
    # every PR/push. Production images are built via Containerfile in
    # publish.yml, not here.
    runs-on: ubuntu-latest
    needs: [lint, test]
    defaults:
      run:
        working-directory: services/elitea-llm-gateway
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: "1.26"
          cache-dependency-path: services/elitea-llm-gateway/go.sum

      - name: Build elitea-llm-gateway
        run: CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o /tmp/elitea-llm-gateway ./cmd/elitea-llm-gateway
