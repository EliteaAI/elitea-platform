// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: elitea/gateway/v1/gateway.proto

package gatewayv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	GatewayService_ChatCompletions_FullMethodName = "/elitea.gateway.v1.GatewayService/ChatCompletions"
	GatewayService_ListModels_FullMethodName      = "/elitea.gateway.v1.GatewayService/ListModels"
	GatewayService_Health_FullMethodName          = "/elitea.gateway.v1.GatewayService/Health"
)

// GatewayServiceClient is the client API for GatewayService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// GatewayService is the gRPC interface exposed by elitea-gateway-python
// (LiteLLM proxy). elitea-main (Go) is the primary caller for LLM inference.
//
// All RPCs in this service are internal (Go → Python). The SPA never calls
// GatewayService directly; it reaches the LLM proxy via the REST API on
// elitea-main, which forwards to GatewayService under the hood.
type GatewayServiceClient interface {
	// Deprecated: Do not use.
	// ChatCompletions is a server-streaming RPC that relays a multi-model chat
	// request to the underlying LLM provider and streams token chunks back to
	// the caller until the response is complete.
	//
	// DEPRECATED (BF0.7 / ADR-0015 §9.4): this RPC was designed for the retired
	// Python LiteLLM adapter. The elitea-main → gateway internal hop is an mTLS
	// HTTP/1.1 streaming reverse proxy, NOT gRPC, so this RPC is stranded. A
	// deterministic usage audit across pylon_indexer and elitea-sdk found NO live
	// caller (only the generated stubs reference it), so per spec-bifrost-migration
	// §6 it is marked deprecated. The RPC and its request/response are retained
	// (deleting them would break the FILE breaking-rule baseline) but must not be
	// used. Their unused field-number tails are `reserved` below to freeze the
	// contract. If a typed internal transport is ever preferred over the reverse
	// proxy (the deferred §9.4 option), lift those reservations and rename this
	// RPC to InternalAgentStream.
	ChatCompletions(ctx context.Context, in *ChatCompletionsRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ChatCompletionsChunk], error)
	// ListModels returns the set of LLM models available to the calling project.
	ListModels(ctx context.Context, in *ListModelsRequest, opts ...grpc.CallOption) (*ListModelsResponse, error)
	// Health returns the liveness status of the gateway service.
	Health(ctx context.Context, in *HealthRequest, opts ...grpc.CallOption) (*HealthResponse, error)
}

type gatewayServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewGatewayServiceClient(cc grpc.ClientConnInterface) GatewayServiceClient {
	return &gatewayServiceClient{cc}
}

// Deprecated: Do not use.
func (c *gatewayServiceClient) ChatCompletions(ctx context.Context, in *ChatCompletionsRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ChatCompletionsChunk], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &GatewayService_ServiceDesc.Streams[0], GatewayService_ChatCompletions_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[ChatCompletionsRequest, ChatCompletionsChunk]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type GatewayService_ChatCompletionsClient = grpc.ServerStreamingClient[ChatCompletionsChunk]

func (c *gatewayServiceClient) ListModels(ctx context.Context, in *ListModelsRequest, opts ...grpc.CallOption) (*ListModelsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListModelsResponse)
	err := c.cc.Invoke(ctx, GatewayService_ListModels_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *gatewayServiceClient) Health(ctx context.Context, in *HealthRequest, opts ...grpc.CallOption) (*HealthResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(HealthResponse)
	err := c.cc.Invoke(ctx, GatewayService_Health_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// GatewayServiceServer is the server API for GatewayService service.
// All implementations must embed UnimplementedGatewayServiceServer
// for forward compatibility.
//
// GatewayService is the gRPC interface exposed by elitea-gateway-python
// (LiteLLM proxy). elitea-main (Go) is the primary caller for LLM inference.
//
// All RPCs in this service are internal (Go → Python). The SPA never calls
// GatewayService directly; it reaches the LLM proxy via the REST API on
// elitea-main, which forwards to GatewayService under the hood.
type GatewayServiceServer interface {
	// Deprecated: Do not use.
	// ChatCompletions is a server-streaming RPC that relays a multi-model chat
	// request to the underlying LLM provider and streams token chunks back to
	// the caller until the response is complete.
	//
	// DEPRECATED (BF0.7 / ADR-0015 §9.4): this RPC was designed for the retired
	// Python LiteLLM adapter. The elitea-main → gateway internal hop is an mTLS
	// HTTP/1.1 streaming reverse proxy, NOT gRPC, so this RPC is stranded. A
	// deterministic usage audit across pylon_indexer and elitea-sdk found NO live
	// caller (only the generated stubs reference it), so per spec-bifrost-migration
	// §6 it is marked deprecated. The RPC and its request/response are retained
	// (deleting them would break the FILE breaking-rule baseline) but must not be
	// used. Their unused field-number tails are `reserved` below to freeze the
	// contract. If a typed internal transport is ever preferred over the reverse
	// proxy (the deferred §9.4 option), lift those reservations and rename this
	// RPC to InternalAgentStream.
	ChatCompletions(*ChatCompletionsRequest, grpc.ServerStreamingServer[ChatCompletionsChunk]) error
	// ListModels returns the set of LLM models available to the calling project.
	ListModels(context.Context, *ListModelsRequest) (*ListModelsResponse, error)
	// Health returns the liveness status of the gateway service.
	Health(context.Context, *HealthRequest) (*HealthResponse, error)
	mustEmbedUnimplementedGatewayServiceServer()
}

// UnimplementedGatewayServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedGatewayServiceServer struct{}

func (UnimplementedGatewayServiceServer) ChatCompletions(*ChatCompletionsRequest, grpc.ServerStreamingServer[ChatCompletionsChunk]) error {
	return status.Error(codes.Unimplemented, "method ChatCompletions not implemented")
}
func (UnimplementedGatewayServiceServer) ListModels(context.Context, *ListModelsRequest) (*ListModelsResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ListModels not implemented")
}
func (UnimplementedGatewayServiceServer) Health(context.Context, *HealthRequest) (*HealthResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Health not implemented")
}
func (UnimplementedGatewayServiceServer) mustEmbedUnimplementedGatewayServiceServer() {}
func (UnimplementedGatewayServiceServer) testEmbeddedByValue()                        {}

// UnsafeGatewayServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to GatewayServiceServer will
// result in compilation errors.
type UnsafeGatewayServiceServer interface {
	mustEmbedUnimplementedGatewayServiceServer()
}

func RegisterGatewayServiceServer(s grpc.ServiceRegistrar, srv GatewayServiceServer) {
	// If the following call panics, it indicates UnimplementedGatewayServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&GatewayService_ServiceDesc, srv)
}

func _GatewayService_ChatCompletions_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(ChatCompletionsRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(GatewayServiceServer).ChatCompletions(m, &grpc.GenericServerStream[ChatCompletionsRequest, ChatCompletionsChunk]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type GatewayService_ChatCompletionsServer = grpc.ServerStreamingServer[ChatCompletionsChunk]

func _GatewayService_ListModels_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListModelsRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(GatewayServiceServer).ListModels(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: GatewayService_ListModels_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(GatewayServiceServer).ListModels(ctx, req.(*ListModelsRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _GatewayService_Health_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HealthRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(GatewayServiceServer).Health(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: GatewayService_Health_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(GatewayServiceServer).Health(ctx, req.(*HealthRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// GatewayService_ServiceDesc is the grpc.ServiceDesc for GatewayService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var GatewayService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "elitea.gateway.v1.GatewayService",
	HandlerType: (*GatewayServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "ListModels",
			Handler:    _GatewayService_ListModels_Handler,
		},
		{
			MethodName: "Health",
			Handler:    _GatewayService_Health_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "ChatCompletions",
			Handler:       _GatewayService_ChatCompletions_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "elitea/gateway/v1/gateway.proto",
}
